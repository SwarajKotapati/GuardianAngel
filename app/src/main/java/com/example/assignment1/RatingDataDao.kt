package com.example.assignment1

import androidx.room.Dao

@Dao
interface RatingDataDao {
//    @Insert(onConflict = OnConflictStrategy.IGNORE)
//    suspend fun insert(ratingData: RatingData)
//
//    @Query("SELECT * FROM ratings_table WHERE entryName = :entryName")
//    suspend fun getRatingForEntry(entryName: String): RatingData?
}
